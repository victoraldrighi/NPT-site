---
phase: 01-foundation-and-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.js
  - netlify.toml
  - .gitignore
  - index.html
  - src/css/tokens.css
  - src/css/base.css
  - src/js/main.js
  - blog/.gitkeep
  - public/favicon.svg
autonomous: true
requirements:
  - IDEN-01
  - IDEN-02
  - IDEN-03
  - IDEN-04
  - INFRA-01
  - INFRA-03

must_haves:
  truths:
    - "CSS custom properties for colors, typography, and spacing are defined in tokens.css and visually applied to the live page"
    - "Inter font loads at weights 400, 500, 600, 700 from self-hosted @fontsource (no Google Fonts CDN)"
    - "Logo SVG renders in the page header"
    - "Stub page layout adapts to 375px, 768px, and 1280px without horizontal overflow"
    - "Vite build produces hashed assets in dist/ directory"
    - "blog/ directory exists in the repository with a .gitkeep file"
  artifacts:
    - path: "src/css/tokens.css"
      provides: "All CSS custom properties (colors, typography, spacing, layout)"
      contains: "--color-brand-primary"
    - path: "src/css/base.css"
      provides: "Minimal CSS reset and global typography"
      contains: "var(--font-family-base)"
    - path: "src/js/main.js"
      provides: "JS entry point with @fontsource/inter imports"
      contains: "@fontsource/inter/700.css"
    - path: "vite.config.js"
      provides: "Vite 6.x multi-page build configuration"
      contains: "rollupOptions"
    - path: "index.html"
      provides: "Stub page with logo, tokens applied, responsive layout"
      contains: "<svg"
    - path: "blog/.gitkeep"
      provides: "Empty blog directory scaffold for v2"
  key_links:
    - from: "index.html"
      to: "src/js/main.js"
      via: "script type=module src"
      pattern: 'src="/src/js/main.js"'
    - from: "src/js/main.js"
      to: "@fontsource/inter"
      via: "ES module imports"
      pattern: "@fontsource/inter/400.css"
    - from: "src/js/main.js"
      to: "src/css/tokens.css"
      via: "CSS import chain"
      pattern: "import.*tokens.css"
    - from: "src/css/base.css"
      to: "src/css/tokens.css"
      via: "CSS @import or var() references"
      pattern: "var(--color-"
---

<objective>
Scaffold the Vite 6.x project, implement the complete CSS design token system, configure @fontsource/inter, create the stub deployment page with logo and responsive layout, and prepare the blog directory for v2.

Purpose: Establish the technical foundation and visual identity that every subsequent phase inherits. All color, typography, spacing, and layout tokens are locked after this plan.
Output: A buildable Vite project with tokens.css, base.css, Inter font, stub index.html with logo, and blog/ scaffold.
</objective>

<execution_context>
@C:/Users/valdrighi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/valdrighi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-and-design-system/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite 6.x project with full file structure</name>
  <files>package.json, vite.config.js, netlify.toml, .gitignore, src/js/main.js, blog/.gitkeep, public/favicon.svg</files>
  <read_first>
    .planning/phases/01-foundation-and-design-system/01-RESEARCH.md
    .planning/phases/01-foundation-and-design-system/01-CONTEXT.md
  </read_first>
  <action>
Create the project from scratch (do NOT use `npm create vite` — it will install Vite 8). Instead:

1. **package.json** — Create manually with these exact contents:
```json
{
  "name": "npt-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}
```

2. Run `npm install --save-dev vite@6.4.1` and `npm install @fontsource/inter` from the repo root (`/c/Users/valdrighi`).

3. **vite.config.js** — Create with multi-page build config:
```javascript
import { dirname, resolve } from 'node:path'
import { fileURLToPath } from 'node:url'
import { defineConfig } from 'vite'

const __dirname = dirname(fileURLToPath(import.meta.url))

export default defineConfig({
  build: {
    rollupOptions: {
      input: {
        main: resolve(__dirname, 'index.html'),
      },
    },
  },
})
```

4. **netlify.toml** — Create at repo root:
```toml
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
```

5. **.gitignore** — Create with:
```
node_modules/
dist/
.DS_Store
*.local
```

6. **src/js/main.js** — Create with font imports and CSS imports:
```javascript
import '@fontsource/inter/400.css'
import '@fontsource/inter/500.css'
import '@fontsource/inter/600.css'
import '@fontsource/inter/700.css'

import '../css/tokens.css'
import '../css/base.css'
```

7. **blog/.gitkeep** — Create empty file at `blog/.gitkeep`.

8. **public/favicon.svg** — Create a minimal NPT-branded favicon SVG using navy color `#1B3A6B`:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <rect width="32" height="32" rx="6" fill="#1B3A6B"/>
  <text x="16" y="22" font-family="sans-serif" font-size="16" font-weight="700" fill="#FFFFFF" text-anchor="middle">N</text>
</svg>
```

9. Create empty directories: `src/css/components/`, `src/assets/logo/`.
  </action>
  <verify>
    <automated>cd /c/Users/valdrighi && node -e "const p=require('./package.json'); console.log(p.name, p.type)" && test -f vite.config.js && test -f netlify.toml && test -f blog/.gitkeep && test -f src/js/main.js && test -f public/favicon.svg && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"name": "npt-site"` and `"type": "module"`
    - `package.json` devDependencies contains `vite` with version starting with `6.`
    - `package.json` dependencies contains `@fontsource/inter`
    - `vite.config.js` contains `rollupOptions` and `resolve(__dirname, 'index.html')`
    - `netlify.toml` contains `publish = "dist"` and `NODE_VERSION = "20"`
    - `src/js/main.js` contains all four lines: `@fontsource/inter/400.css`, `@fontsource/inter/500.css`, `@fontsource/inter/600.css`, `@fontsource/inter/700.css`
    - `src/js/main.js` imports `tokens.css` and `base.css`
    - `blog/.gitkeep` exists as a file
    - `.gitignore` contains `node_modules/` and `dist/`
    - `public/favicon.svg` exists
    - `node_modules/vite/` directory exists (npm install succeeded)
    - `node_modules/@fontsource/inter/` directory exists
  </acceptance_criteria>
  <done>Vite 6.4.1 project is initialized with all supporting files, @fontsource/inter installed, blog scaffold in place, netlify.toml configured.</done>
</task>

<task type="auto">
  <name>Task 2: Create CSS design token system and base styles</name>
  <files>src/css/tokens.css, src/css/base.css</files>
  <read_first>
    .planning/phases/01-foundation-and-design-system/01-RESEARCH.md
    .planning/phases/01-foundation-and-design-system/01-CONTEXT.md
  </read_first>
  <action>
1. **src/css/tokens.css** — Create with the complete two-layer token system. Use EXACTLY these values (from CONTEXT.md locked decisions):

```css
:root {
  /* --- PRIMITIVE TOKENS --- */
  --primitive-navy: #1B3A6B;
  --primitive-amber: #F5A623;
  --primitive-off-white: #F8F9FA;
  --primitive-white: #FFFFFF;
  --primitive-dark: #1A1A2E;
  --primitive-muted: #6B7280;
  --primitive-green: #10B981;

  /* --- SEMANTIC COLOR TOKENS --- */
  --color-brand-primary: var(--primitive-navy);
  --color-brand-accent: var(--primitive-amber);
  --color-bg-page: var(--primitive-off-white);
  --color-bg-surface: var(--primitive-white);
  --color-text-body: var(--primitive-dark);
  --color-text-muted: var(--primitive-muted);
  --color-feedback-success: var(--primitive-green);

  /* --- TYPOGRAPHY TOKENS --- */
  --font-family-base: 'Inter', system-ui, sans-serif;
  --font-weight-regular: 400;
  --font-weight-medium: 500;
  --font-weight-semibold: 600;
  --font-weight-bold: 700;

  --font-size-xs: 0.75rem;
  --font-size-sm: 0.875rem;
  --font-size-base: 1rem;
  --font-size-lg: 1.125rem;
  --font-size-xl: 1.25rem;
  --font-size-2xl: 1.5rem;
  --font-size-3xl: 1.875rem;
  --font-size-4xl: 2.25rem;
  --font-size-5xl: 3rem;

  --line-height-body: 1.5;
  --line-height-heading: 1.2;

  /* --- SPACING TOKENS (4px base) --- */
  --space-1: 0.25rem;
  --space-2: 0.5rem;
  --space-3: 0.75rem;
  --space-4: 1rem;
  --space-6: 1.5rem;
  --space-8: 2rem;
  --space-12: 3rem;
  --space-16: 4rem;
  --space-24: 6rem;
  --space-32: 8rem;

  /* --- LAYOUT TOKENS --- */
  --max-width-content: 75rem;
  --bp-tablet: 48rem;
  --bp-desktop: 80rem;

  /* --- COMPONENT TOKENS --- */
  --radius-sm: 0.25rem;
  --radius-md: 0.5rem;
  --radius-lg: 0.75rem;
  --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
  --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1);
  --transition-base: 150ms ease-in-out;
}
```

2. **src/css/base.css** — Create with minimal reset and global typography. Do NOT use `@import './tokens.css'` here (tokens.css is already imported via main.js before base.css). Apply tokens directly:

```css
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: 100%;
  -webkit-text-size-adjust: 100%;
}

body {
  font-family: var(--font-family-base);
  font-size: var(--font-size-base);
  line-height: var(--line-height-body);
  color: var(--color-text-body);
  background-color: var(--color-bg-page);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

img, video, svg {
  display: block;
  max-width: 100%;
}

h1, h2, h3, h4, h5, h6 {
  line-height: var(--line-height-heading);
  font-weight: var(--font-weight-bold);
}

a {
  color: var(--color-brand-primary);
  text-decoration: none;
}

a:hover {
  color: var(--color-brand-accent);
}
```

IMPORTANT: Do NOT use CSS custom properties inside @media queries. Breakpoints in media queries MUST use raw rem values: `48rem` for tablet, `80rem` for desktop. The `--bp-tablet` and `--bp-desktop` tokens are documentation-only references.
  </action>
  <verify>
    <automated>cd /c/Users/valdrighi && grep -c "color-brand-primary" src/css/tokens.css && grep -c "font-family-base" src/css/tokens.css && grep "var(--color-text-body)" src/css/base.css && grep "var(--font-family-base)" src/css/base.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `src/css/tokens.css` contains `--primitive-navy: #1B3A6B`
    - `src/css/tokens.css` contains `--primitive-amber: #F5A623`
    - `src/css/tokens.css` contains `--color-brand-primary: var(--primitive-navy)`
    - `src/css/tokens.css` contains `--color-brand-accent: var(--primitive-amber)`
    - `src/css/tokens.css` contains `--color-bg-page: var(--primitive-off-white)`
    - `src/css/tokens.css` contains `--font-family-base: 'Inter', system-ui, sans-serif`
    - `src/css/tokens.css` contains all 4 font weights: `--font-weight-regular: 400`, `--font-weight-medium: 500`, `--font-weight-semibold: 600`, `--font-weight-bold: 700`
    - `src/css/tokens.css` contains spacing tokens from `--space-1` through `--space-32`
    - `src/css/tokens.css` contains `--max-width-content: 75rem`
    - `src/css/tokens.css` contains `--radius-sm`, `--radius-md`, `--radius-lg`, `--shadow-sm`, `--shadow-md`, `--transition-base`
    - `src/css/base.css` contains `var(--font-family-base)` in body rule
    - `src/css/base.css` contains `var(--color-text-body)` in body rule
    - `src/css/base.css` contains `var(--color-bg-page)` in body rule
    - `src/css/base.css` contains `box-sizing: border-box` in universal selector
    - `src/css/base.css` does NOT contain `@media (min-width: var(` (no CSS vars in media queries)
  </acceptance_criteria>
  <done>Complete CSS token system defined in tokens.css with two-layer architecture (primitives + semantic). Base.css provides minimal reset and applies tokens to global elements.</done>
</task>

<task type="auto">
  <name>Task 3: Create stub index.html with logo and responsive layout</name>
  <files>index.html</files>
  <read_first>
    .planning/phases/01-foundation-and-design-system/01-CONTEXT.md
    .planning/phases/01-foundation-and-design-system/01-RESEARCH.md
    src/css/tokens.css
    src/css/base.css
  </read_first>
  <action>
Create `index.html` at the repo root. This is a stub/proof-of-deploy page (NOT a real landing page). It must:

1. Use `lang="pt-BR"` on the html element
2. Include proper meta viewport tag
3. Reference `/src/js/main.js` as `type="module"` script
4. Include an inline SVG logo in the header with `aria-label="NPT Engenharia"` and `role="img"`. Since the client's actual logo SVG is not yet available, create a placeholder text-based SVG that says "NPT" using the navy color. The SVG must use `currentColor` for fill so CSS can control its color.
5. Show message "Site em desenvolvimento — em breve." in pt-BR
6. Apply design tokens visually: navy header background with white logo text, off-white page background, body text in dark color
7. Be responsive at 375px, 768px, 1280px without horizontal overflow

Use a `<style>` block in the HTML for stub-specific styles (these are temporary and will be replaced in Phase 2). Use raw rem values for media queries, NOT CSS custom property references.

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>NPT Engenharia</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
</head>
<body>
  <header class="site-header">
    <div class="container">
      <svg class="site-logo" aria-label="NPT Engenharia" role="img" viewBox="0 0 120 40" width="120" height="40">
        <text x="0" y="30" font-family="sans-serif" font-size="28" font-weight="700" fill="currentColor">NPT</text>
      </svg>
    </div>
  </header>
  <main class="main-content">
    <div class="container">
      <h1>NPT Engenharia</h1>
      <p>Site em desenvolvimento — em breve.</p>
    </div>
  </main>
  <footer class="site-footer">
    <div class="container">
      <p>&copy; 2026 NPT Engenharia. Todos os direitos reservados.</p>
    </div>
  </footer>
  <script type="module" src="/src/js/main.js"></script>

  <style>
    /* Stub page styles — temporary, replaced in Phase 2 */
    .site-header {
      background-color: var(--color-brand-primary);
      padding: var(--space-4);
    }
    .site-header .site-logo {
      color: var(--color-bg-surface); /* white logo on navy */
    }
    .container {
      width: 100%;
      max-width: var(--max-width-content);
      margin-left: auto;
      margin-right: auto;
      padding-left: var(--space-4);
      padding-right: var(--space-4);
    }
    .main-content {
      padding: var(--space-16) 0;
      text-align: center;
    }
    .main-content h1 {
      font-size: var(--font-size-4xl);
      color: var(--color-brand-primary);
      margin-bottom: var(--space-4);
    }
    .main-content p {
      font-size: var(--font-size-lg);
      color: var(--color-text-muted);
    }
    .site-footer {
      background-color: var(--color-brand-primary);
      color: var(--color-bg-surface);
      padding: var(--space-6);
      text-align: center;
      font-size: var(--font-size-sm);
    }
    /* Tablet breakpoint — raw rem, NOT var(--bp-tablet) */
    @media (min-width: 48rem) {
      .container {
        padding-left: var(--space-6);
        padding-right: var(--space-6);
      }
    }
    /* Desktop breakpoint — raw rem, NOT var(--bp-desktop) */
    @media (min-width: 80rem) {
      .container {
        padding-left: var(--space-8);
        padding-right: var(--space-8);
      }
    }
  </style>
</body>
</html>
```

After creating the HTML, run `npm run build` to verify the Vite build succeeds and produces hashed output in `dist/`.
  </action>
  <verify>
    <automated>cd /c/Users/valdrighi && grep 'lang="pt-BR"' index.html && grep '<svg' index.html && grep 'src="/src/js/main.js"' index.html && grep "em breve" index.html && npm run build && ls dist/assets/ && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `index.html` contains `lang="pt-BR"` on the html element
    - `index.html` contains `<meta name="viewport"` tag
    - `index.html` contains an `<svg` element with `aria-label="NPT Engenharia"` and `role="img"`
    - `index.html` contains `<script type="module" src="/src/js/main.js">`
    - `index.html` contains the text "Site em desenvolvimento" in pt-BR
    - `index.html` contains `var(--color-brand-primary)` applied to header background
    - `index.html` media queries use `48rem` and `80rem` raw values, NOT `var(--bp-tablet)` or `var(--bp-desktop)`
    - `npm run build` exits with code 0
    - `dist/` directory is created with `index.html` and `dist/assets/` containing hashed JS/CSS files
  </acceptance_criteria>
  <done>Stub page renders with navy header, white logo, Inter font, off-white background. Responsive at all 3 breakpoints. Vite build produces hashed output in dist/.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` exits 0 and produces `dist/index.html` + hashed assets in `dist/assets/`
2. `npm run preview` serves the stub page locally — visual check: navy header, Inter font, NPT logo visible
3. `test -f blog/.gitkeep` confirms blog directory scaffold
4. `grep "#1B3A6B" src/css/tokens.css` confirms locked color palette
5. `grep "@fontsource/inter/700.css" src/js/main.js` confirms all font weights imported
6. No `@media (min-width: var(` pattern anywhere in the codebase (CSS vars not used in media queries)
</verification>

<success_criteria>
- Vite 6.4.1 build pipeline produces hashed assets without errors
- tokens.css contains the complete two-layer design system (7 colors, 9 font sizes, 10 spacing values, 3 radii, 2 shadows)
- Inter font self-hosted at 4 weights (400, 500, 600, 700) via @fontsource
- Stub page visually applies tokens (navy header, amber accent available, off-white background, Inter typography)
- SVG logo placeholder renders in header at all breakpoints
- Responsive layout works at 375px, 768px, 1280px without overflow
- blog/.gitkeep exists for INFRA-03
- netlify.toml configured with publish=dist and NODE_VERSION=20
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-design-system/01-01-SUMMARY.md`
</output>
