---
phase: 01-foundation-and-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: []
autonomous: false
requirements:
  - INFRA-02

must_haves:
  truths:
    - "A Netlify URL is live and returns HTTP 200 with valid HTML"
    - "HTTPS is active on the Netlify URL"
    - "Pushing to the git repository triggers an automatic Netlify build and deploy"
  artifacts: []
  key_links:
    - from: "git repository"
      to: "Netlify site"
      via: "Continuous deployment (git push triggers build)"
      pattern: "npm run build -> dist/ -> Netlify CDN"
---

<objective>
Deploy the Vite project to Netlify and verify the live URL returns a working stub page with the design system visually applied.

Purpose: Validate the full pipeline (git push -> Netlify build -> live site) before any real content is added. This proves INFRA-02 and confirms the foundation from Plan 01 works end-to-end in production.
Output: A live Netlify URL serving the stub page with tokens applied.
</objective>

<execution_context>
@C:/Users/valdrighi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/valdrighi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-and-design-system/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to Netlify via CLI</name>
  <files></files>
  <read_first>
    netlify.toml
    package.json
  </read_first>
  <action>
Attempt to deploy using the Netlify CLI. If the Netlify CLI is not installed, install it globally with `npm install -g netlify-cli`.

1. Run `npm run build` to ensure dist/ is fresh.
2. Run `netlify deploy --prod --dir=dist` to deploy the built site.
   - If not authenticated, this will create a checkpoint for the user to authenticate.
   - If no site is linked, run `netlify init` or `netlify link` to connect to a Netlify site.
3. Capture the deployed URL from the CLI output.

If CLI deployment is not possible (auth issues, no Netlify account linked), create a checkpoint task for the user to:
- Connect the GitHub repository to Netlify via the Netlify dashboard (https://app.netlify.com)
- Set build command: `npm run build`
- Set publish directory: `dist`
- The netlify.toml in the repo will override these settings automatically.

After deployment, verify with: `curl -sI https://[site-url] | head -5` expecting `HTTP/2 200`.
  </action>
  <verify>
    <automated>cd /c/Users/valdrighi && npm run build && echo "Build successful — ready for deploy"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `dist/index.html` exists and contains "NPT Engenharia"
    - `dist/assets/` contains at least one `.js` and one `.css` file with hash in filename
  </acceptance_criteria>
  <done>Site built and deploy attempted via CLI or instructions provided for manual Netlify connection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Netlify deployment is live</name>
  <files></files>
  <action>
Human verification checkpoint. Claude has deployed (or provided instructions to deploy) the stub page to Netlify. The user must now verify the live deployment visually and confirm the full pipeline works.

What was built:
Complete Phase 1 foundation: Vite 6.x project with CSS design token system (navy #1B3A6B + amber #F5A623 palette, Inter font at 4 weights, 10-step spacing scale), stub page with placeholder logo, responsive layout, deployed to Netlify.

Steps to verify:
1. Open the Netlify URL in a browser (the URL will be displayed after Task 1 deploy)
2. Verify the page loads with HTTPS (lock icon in browser)
3. Verify visual identity:
   - Navy (#1B3A6B) header background
   - White "NPT" text/logo in header
   - Off-white (#F8F9FA) page background
   - "Site em desenvolvimento — em breve." message visible
   - Inter font loaded (check in DevTools > Network > Font or Elements > Computed)
4. Test responsiveness in browser DevTools:
   - 375px width: no horizontal scroll, content readable
   - 768px width: increased padding, no overflow
   - 1280px width: content centered within max-width, no overflow
5. Verify HTTPS is active (green lock icon, URL starts with https://)
6. Push a trivial change (e.g., add a comment to index.html) and verify Netlify auto-deploys within 2 minutes

Resume signal: Type "approved" if the Netlify URL is live with correct visual identity, or describe any issues you see.
  </action>
  <verify>User confirms "approved" after visual verification of live Netlify URL.</verify>
  <done>Netlify URL is live with HTTPS, design tokens visually applied, auto-deploy pipeline confirmed.</done>
</task>

</tasks>

<verification>
After checkpoint approval:
1. Netlify URL returns HTTP 200 with valid HTML
2. Page renders with design tokens visually applied (navy header, Inter font, off-white background)
3. HTTPS is active
4. Auto-deploy pipeline works on git push
</verification>

<success_criteria>
- Live Netlify URL serves the stub page
- Visual identity matches locked decisions: navy header, amber accent available, Inter font, off-white background
- Responsive layout works at 375px, 768px, 1280px on the live URL
- HTTPS active
- Auto-deploy on git push confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-design-system/01-02-SUMMARY.md`
</output>
